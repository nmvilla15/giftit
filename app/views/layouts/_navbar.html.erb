<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
  <a class="navbar-brand" href="/"><%= image_tag("GiftIt.jpeg", style: "width: 75px; height: 70px;")%></a>
  <!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon">asffasjhfasfasnjfnka sjnfkajsnfaksjfnasnakjs</span>
  </button> Delete if not needed, i dont know why this button is here. -->
  <div class="collapse navbar-collapse d-flex justify-content-between " id="navbarNav">
    <ul class="navbar-nav col-10">
      <li class="nav-item">
        <a class="nav-link" href="/posts#index">Gifts</a>
      </li>
      <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Gift Categories
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% Category.all.each do |cat| %>
            <li><a class="dropdown-item"><%= link_to cat.name, posts_path(category: cat.name), class:"nav-link text-center" %></a></li>
            <% end %>
          </ul>
    </ul>

    <ul class="navbar-nav col-2 text-center d-flex justify-content-center ">
      <li class="nav-item dropdown">
          <% if current_user  %>
          <a class="nav-link dropdown-toggle " href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
               Hi, <strong><%= current_user.username %> <br>Account</strong></a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "My Profile", user_path(current_user.id), class:"nav-link" %>
            <%= link_to "My Conversations", conversations_path, class:"nav-link" %>
            <%= link_to "Log Out", logout_path, class:"nav-link" %>
          <% else %>
            <%= link_to "Log In", login_path, class: "btn btn-info" %>
            <%= link_to "Sign Up", signup_path, class: "btn btn-info" %>
          <% end %>
      </li>
    </ul>
  </div>
</nav>
