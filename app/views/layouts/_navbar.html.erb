<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
  <a class="navbar-brand" href="/"><%= image_tag("GiftIt.jpeg", style: "width: 75px; height: 70px;")%></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">  
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item">
        <a class="nav-link" href="/posts#index">Gifts</a>
      </li>
      <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Gift Categories
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% Category.all.each do |cat| %>
            <li><a class="dropdown-item"><%= link_to cat.name, posts_path(category: cat.name) %></a></li>
            <% end %>
          </ul>
          <% if current_user %>
        <li class="nav-item">
        <%= link_to "My Profile", user_path(current_user.id) %>
        </li>
        <% end %>
    </ul>
    <ul class="navbar-nav ms-auto">
      <li class="nav-item">
        <a class="nav-link">
         <% if current_user %>
            Logged in as <%= current_user.username %>
            <%= link_to "My Conversations", conversations_path, class: "btn btn-secondary" %>
            <%= link_to "Log Out", logout_path, class: "btn btn-secondary" %>
          <% else %>
            <%= link_to "Log In", login_path, class: "btn btn-info" %>
            <%= link_to "Sign Up", signup_path, class: "btn btn-info" %>
          <% end %>
        </a>
      </li>
    </ul>
  </div>
</nav>